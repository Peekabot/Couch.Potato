# Core scraping — curl_cffi impersonates a real Chrome TLS fingerprint,
# which bypasses most basic bot-detection (Akamai, Cloudflare level 1).
curl_cffi>=0.6.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Data I/O (CSV/DataFrame helpers)
pandas>=2.0.0

# dotenv for optional proxy/output-dir config via .env file
python-dotenv>=1.0.0
