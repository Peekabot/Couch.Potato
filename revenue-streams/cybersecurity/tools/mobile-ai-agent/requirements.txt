# Mobile AI Recon Agent - Python Dependencies
# Compatible with PythonAnywhere free tier

# Web Framework
flask==3.0.0

# HTTP Requests
requests==2.31.0

# Async operations (optional)
aiohttp==3.9.1

# DNS and subdomain tools
dnspython==2.4.2

# Command line parsing
argparse==1.4.0

# For web scraping (optional)
beautifulsoup4==4.12.2
lxml==4.9.3

# Date/time utilities
python-dateutil==2.8.2

# API integrations
urllib3==2.1.0

# Security tools (lightweight alternatives)
# Note: Heavy tools like nmap require system installation
# Use API-based alternatives for PythonAnywhere

# ===================================================================
# AI / Devstral Vibe Integration (OPTIONAL)
# ===================================================================
# These are only needed if you enable AI features (ai.enabled = true)
# For PythonAnywhere free tier, use API mode instead of local models
# ===================================================================

# Option 1: Local Model Inference (requires paid tier, lots of RAM)
# Only install if you have 4GB+ RAM and want to run models locally
# transformers==4.36.0
# torch==2.1.2+cpu  # CPU-only version
# accelerate==0.25.0
# sentencepiece==0.1.99  # For some tokenizers
# protobuf==4.25.1

# Option 2: API-based AI (works on free tier!) - RECOMMENDED
# No extra packages needed - uses requests library above
# Supported APIs:
#   - Mistral API (api.mistral.ai) - requires API key
#   - Ollama (localhost:11434) - requires local Ollama install
#   - HuggingFace Inference API - requires HF token

# Optional: For better text processing
# tiktoken==0.5.2  # OpenAI tokenizer (if using OpenAI-compatible APIs)

# Email
# (using standard library smtplib, no extra dependency)

# Logging (using standard library)

# JSON (using standard library)
